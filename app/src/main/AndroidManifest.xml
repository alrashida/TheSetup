<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mysus.myfav">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/keyboard"
        android:label="The Setup"
        android:roundIcon="@drawable/gradient_endcolor"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyFav">
        <activity
            android:name=".DetailActivityComp"
            android:exported="true" />
        <activity
            android:name=".DetailActivitySetup"
            android:exported="true" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>